import React from 'react';
import ForceGraph2D from 'react-force-graph-2d'

const Graph = (config) => {
    return (
        <ForceGraph2D
            {...config}
        />
    )
}

export default Graph;